# The _new advisor funnel was archived on 17 July 2026 and its pages no longer
# exist, so those URLs now return 404.
#
# The Disallow rules that used to sit here for /home_new, /start_new,
# /questionnaire_new, /booking_new, /advisor-booking_new, /guides_new,
# /terms_new and /privacy-policy_new have been REMOVED ON PURPOSE. Disallow
# stops a crawler fetching a URL at all, which would stop it ever seeing the
# 404, so anything already indexed could linger. Letting crawlers fetch and
# receive the 404 is what actually gets them dropped from the index.

User-agent: *

# The purchased report renders from the buyer's own browser storage and has no
# meaning to a crawler.
Disallow: /v2-success.html

Allow: /
